name: Python app.py

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version:  3.12.0  # Replace 'x' with the desired Python version

    
    - name: Install dependencies
      run: echo "No dependencies to install"

    - name: Run tests
      run: |
        python -m app.py  # Replace with your test command

